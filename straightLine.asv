function [ y ] = straightLine( x1, y1, x2, y2, newX )
%UNTITLED2 Summary of this function goes here
%   Detailed explanation goes here

y1 = get(pjHandle, 'YData');
x1 = get(pjHandle, 'XData');
y2 = get(enemyHandle, 'YData');
x = get(enemyHandle, 'XData');

m = (y1(1)-y2(1))/(x1(1)-xene(1));

yaux = y2(1);
xaux = xene(1);
b = yaux-(m*xaux);

y = (m*newX) + b;

end

